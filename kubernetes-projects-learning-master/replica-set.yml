apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: cloudinstitute-replicaset
spec:
  replicas: 4
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: nginx
        image: nginx
